# Typecheck and unit tests before anything leaves the machine. Full lint, format check, coverage, and build run in CI.
cd frontend && npm run typecheck && npm run test:run && cd ..
cd backend; PY=.venv/bin/python; [ -x "$PY" ] || PY=python3; $PY -m pytest -q
